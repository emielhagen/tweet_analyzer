function addTweets(reviewHTML) {
  const tweets = document.getElementById('tweets');
  tweets.innerHTML = null;
  tweets.insertAdjacentHTML('afterEnd', reviewHTML);
}

addTweets('<%= j render "tweets/tweet_partial" %>');
